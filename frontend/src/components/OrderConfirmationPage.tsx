import React from 'react';
import { Typography, Container } from '@mui/material';

const OrderConfirmationPage: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">✅ אישור הזמנה</Typography>
    </Container>
  );
};

export default OrderConfirmationPage;